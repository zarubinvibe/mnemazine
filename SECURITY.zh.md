# 安全

<p align="center"><img src="docs/assets/pantheon/doc-security.png" alt="桌上一只封着金印的白色大理石匣，蓝线从收件托盘绕回完成的石板堆，另有一条线停在作为边界的大理石板前" width="100%"></p>

## 你的材料去了哪里

Mnemazine 在本地运行，这句话你可以自己验证，而不必只是相信。截图识别、文档解析、语音转写和哈希
都在你自己的机器上完成。知识库就是你磁盘上的一个文件夹，里面是普通的 Markdown 文件。没有任何东西
上传给我们，因为并不存在「我们」：这个项目没有服务器，没有账号，也没有遥测。

只有一件事会离开你的机器，而且要由你自己打开。当你接入 LLM 提供方时，正在处理的材料文本会发给它，
由它整理成笔记。本地识别与解析先做，所以送出去的是你在提取缓存里可以先读一遍的文本。不接提供方，
流水线照样能跑，读取交给本地引擎。

## 如何报告问题

请通过本仓库的 GitHub security advisories 私下报告安全问题。如果内容里能看到密钥、路径或别人的
私人材料，不要开公开 issue。若你无法使用 advisory，可以开一个 issue，只说明你有一份报告以及如何
联系你。

## 永远不要公开

- API 密钥；
- OAuth 令牌；
- SSH 密钥；
- 浏览器 cookie；
- 私有知识库内容；
- 私人截图；
- 内部主机名；
- 个人绝对路径。

## 负责检查的门禁

发布之前，项目会跑自己的门禁：

```bash
npm run release-check
```

它会执行语法检查、安全自测、`npm audit`，以及针对危险的 agent 与 SSH 参数、URL 中夹带凭据的静态
扫描。它还会扫描本地提取缓存里像令牌的字符串：一张截图或一份 PDF 可能带着凭据，否则这些凭据会流进
合成出来的笔记。一旦发现被捕获的密钥，门禁即报红。
